name: movies_bloc
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^7.0.0
  http: ^0.13.2
  equatable: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.1.2
  bloc_test: ^8.0.0
  data_faker: ^0.0.3
  mocktail_image_network: ^0.1.0

flutter:
  uses-material-design: true

  assets:
    - assets/